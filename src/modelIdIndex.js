function findModelId(make, model){
    make = req.params.make;
    model = req.params.model;
    switch (make, model) {
        case "alfa_romeo", "4c":
            return 
            break;
    
        default:
            break;
    }
}